syntax = "proto3";
package proto;

option go_package = 'proto';

message OpRequest {
    string table = 1;
    bytes reqBody = 3;
}

message OpResponse {
    bytes value = 1;
}


service airdb {
    rpc Put (OpRequest) returns (OpResponse);
    rpc Get (OpRequest) returns (OpResponse);
}
